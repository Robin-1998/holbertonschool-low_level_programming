#include "main.h"

/**
 * print_last_digit.c - print the last digit of a number
 * @n : value of the last digit
 * Return: Always 0.
 */

int print_last_digit(int n)
{
	n %= 10;
	if (n < 0)
	{
		n *= -1;
	}
	_putchar('0' + n);
	return (n);
}
