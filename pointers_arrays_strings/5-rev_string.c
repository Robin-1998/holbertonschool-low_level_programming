#include "main.h"

/**
 * rev_string  - function that prints a string, followed by a new line
 * @s : reverse
 * Return: Always 0.
 */

void rev_string(char *s)
{
	int i;
	int *j= &i;

	for (i = 0; i[s] != '\0'; i++);
	if (i[s] > 9)
	{
		for (i = 0; i);
	}
}
