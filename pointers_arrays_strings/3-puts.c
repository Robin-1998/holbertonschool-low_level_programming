#include "main.h"

/**
 * _puts - function that prints a string, followed by a new line
 * @str : string
 * Return: Always 0.
 */

void _puts(char *str)
{
/*
 * initiliation de variable
*/
	int i = 0;

	while (i[str] != '\0')
	{
		_putchar(i[str]);
		i++;
	}
	_putchar('\n');
}
