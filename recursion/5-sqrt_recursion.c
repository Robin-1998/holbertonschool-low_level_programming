#include "main.h"

/**
 * _sqrt_recursion - function qui retourne la racine carrée d'un nombre
 * @n :
 * Return: Always 0.
 */
int _sqrt_recursion(int n)
{
	if (n * n == 0)
	{
		return (n);
	}
	else
	{
		return (-1);
	}
}
