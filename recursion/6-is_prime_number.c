#include "main.h"

/**
 * is_prime_number - function qui renvoie 1 si l'entier est un nombre entier
 * sinon 0
 * @n : variable de l'entier
 * Return: Always 0.
 */

int is_prime_number(int n)
{
	int i;
	if (i % i > n)
	{
		return (1);
	}
	else
	{
		return (0);
	}
}
