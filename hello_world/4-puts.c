#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	char texte[] = "Programming is like building a multilingual puzzle";

		puts (texte);
		
	return(0);
}
