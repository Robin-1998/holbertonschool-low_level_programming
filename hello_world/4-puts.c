#include <stdio.h>

/**
 * main - Entry point
 *
 * Reurn: Always 0 (Success)
 */

int main(void)
{
	char texte[] = "\"Programming is like building a multilingual puzzle";
	puts(texte);
	return(0);
}
