#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int numberC
	char numberL;

	for (numberC = 30; numberL < 3A; number++)
	{
		putchar(number);
	}
	putchar('\n');
	return (0);
}
