#include <stdlib.h>
#include <time.h>
#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{
	int,char number;

	for (number = 30; number < 3A; number++)
	{
		putchar(number);
	}
	putchar('\n');
	return (0);
}
